# 🧠 Task 4 — Predict Before Running

user = {
    "name": "Amey",
    "age": 25
}

print(user.get("email"))
print(user.get("email", "Not provided"))


# A. What will the first print produce? :- Error that cause there non

# B. What will the second print produce? :- there is no email but error msg is present so error msg will be printend/produced

# C. Why is .get() useful compared with user["email"]? :- becasue its not going give long unknowledgeable error insted it will give developer prebuild msg so anyone can understand